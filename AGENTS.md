# 提交信息规范

本规范适用于本仓库内的所有 Git 提交。

## 标题格式

使用 `type: 中文说明`；需要明确模块时可使用 `type(scope): 中文说明`。

- `type` 使用小写英文，冒号使用半角 `:`，冒号后保留一个空格。
- 说明使用简体中文，技术名词、文件名和产品名称可保留英文。
- 用“新增”“修复”“优化”“移除”等动词描述实际变化，避免“更新代码”“一些修改”等模糊表述。
- 标题简洁，建议不超过 72 个字符，末尾不加句号。
- `scope` 可省略；使用时应为简短的英文模块名，如 `map`、`models`、`data`、`docs`。

## 类型选择

| 类型 | 用途 |
| --- | --- |
| `feat` | 新增功能、地标、数据或用户可见能力 |
| `fix` | 修复错误或不符合预期的行为 |
| `perf` | 性能、加载速度或资源体积优化 |
| `refactor` | 不改变功能行为的代码重构 |
| `docs` | 文档、说明与协作规范调整 |
| `style` | 不影响行为的代码格式调整 |
| `test` | 测试新增或调整 |
| `build` | 构建流程或依赖调整 |
| `ci` | 持续集成与自动化流水线调整 |
| `chore` | 不属于以上类型的维护事项 |
| `revert` | 撤销已有提交 |

按提交的主要目的选择类型；功能改动附带文档时仍使用 `feat` 或 `fix`。

## 提交内容与正文

- 每次提交聚焦一个目的，提交信息必须与实际差异一致。
- 简单改动只写标题；复杂改动在空行后用中文说明原因、关键变化及必要的验证结果。
- 只记录实际执行的验证，不声称未运行的检查已经通过。
- 不兼容变更使用 `type!: 中文说明` 或 `type(scope)!: 中文说明`，并在正文中用 `BREAKING CHANGE:` 说明影响和迁移方式。
- 撤销提交时在正文中注明被撤销提交的哈希与原因。

## 示例

```text
feat: 新增广西体育中心精细地标模型
fix(map): 修复手机端地标点击定位偏移
perf(models): 减少城市模型加载体积
docs: 新增提交信息规范
```
